# The `claude.ai` memory system

(**to do:** investigate how this works.  which claude model runs the memory
synthesis job, and what are its instructions?  it appears that conversations are
individually summarised on an ongoing basis, even though the overall ‘memory
synthesis’ only happens once every 24 hours.)
